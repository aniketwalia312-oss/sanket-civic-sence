diff --git a/src/routes/admin.dashboard.tsx b/src/routes/admin.dashboard.tsx
index 26f4a25..68cd6d7 100644
--- a/src/routes/admin.dashboard.tsx
+++ b/src/routes/admin.dashboard.tsx
@@ -2,7 +2,9 @@ import { createFileRoute, Link } from "@tanstack/react-router";
 import { useQuery, useQueryClient } from "@tanstack/react-query";
 import { useServerFn } from "@tanstack/react-start";
 import { toast } from "sonner";
+import { ShieldCheck } from "lucide-react";
 import { AppShell } from "@/components/sanket/AppShell";
+import { PageHeader } from "@/components/sanket/PageHeader";
 import { IssueMap } from "@/components/sanket/IssueMap";
 import { Chip, StatusBadge } from "@/components/sanket/StatusBadge";
 import { Button } from "@/components/ui/button";
@@ -91,10 +93,12 @@ function AdminDashboard() {
   return (
     <AppShell>
       <div className="mx-auto max-w-6xl space-y-6 px-4 py-8">
-        <header>
-          <h1 className="text-2xl font-semibold tracking-tight">Command centre</h1>
-          <p className="text-sm text-muted-foreground">Live municipal telemetry and AI integrity oversight.</p>
-        </header>
+        <PageHeader
+          icon={ShieldCheck}
+          eyebrow="Municipal oversight"
+          title="Command centre"
+          description="Live municipal telemetry and AI integrity oversight."
+        />
 
         <div className="grid gap-3 sm:grid-cols-4">
           <Metric label="Total tickets" value={String(issues.length)} />
diff --git a/src/routes/auth.tsx b/src/routes/auth.tsx
index 7067fdc..97c6a13 100644
--- a/src/routes/auth.tsx
+++ b/src/routes/auth.tsx
@@ -1,4 +1,4 @@
-import { createFileRoute, useNavigate } from "@tanstack/react-router";
+import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
 import { useEffect, useState } from "react";
 import { useServerFn } from "@tanstack/react-start";
 import { toast } from "sonner";
@@ -105,10 +105,16 @@ function AuthPage() {
   };
 
   return (
-    <div className="flex min-h-screen items-center justify-center bg-secondary/40 px-4 py-10">
+    <div className="flex min-h-screen items-center justify-center bg-gradient-to-b from-secondary/60 via-background to-background px-4 py-10">
       <div className="w-full max-w-md space-y-6">
         <div className="text-center">
-          <div className="mx-auto flex h-12 w-12 items-center justify-center rounded-xl bg-primary text-primary-foreground">
+          <Link
+            to="/"
+            className="inline-flex items-center gap-2 rounded-full border bg-card/90 px-3 py-1.5 text-xs font-medium shadow-sm backdrop-blur"
+          >
+            <Radar className="h-3.5 w-3.5 text-accent" /> Civic issues, tracked to resolution
+          </Link>
+          <div className="mx-auto mt-4 flex h-12 w-12 items-center justify-center rounded-xl bg-primary text-primary-foreground shadow-sm">
             <Radar className="h-6 w-6" />
           </div>
           <h1 className="mt-3 text-2xl font-semibold">Sanket access</h1>
diff --git a/src/routes/challenges.$id.tsx b/src/routes/challenges.$id.tsx
index 02faf98..4900b30 100644
--- a/src/routes/challenges.$id.tsx
+++ b/src/routes/challenges.$id.tsx
@@ -131,7 +131,7 @@ function ChallengeDetail() {
   return (
     <AppShell>
       <div className="mx-auto max-w-5xl space-y-6 px-4 py-8">
-        <div className="space-y-3">
+        <div className="space-y-3 rounded-xl border bg-gradient-to-r from-secondary/70 via-secondary/25 to-transparent px-5 py-6 sm:px-7 sm:py-7">
           <div className="flex flex-wrap items-center gap-2">
             <ChallengeBadge status={challenge.status} />
             <Chip tone="info">{challenge.domain}</Chip>
diff --git a/src/routes/challenges.index.tsx b/src/routes/challenges.index.tsx
index 12f45e4..e074a10 100644
--- a/src/routes/challenges.index.tsx
+++ b/src/routes/challenges.index.tsx
@@ -3,6 +3,7 @@ import { useQuery } from "@tanstack/react-query";
 import { useMemo, useState } from "react";
 import { Lightbulb, Plus } from "lucide-react";
 import { AppShell } from "@/components/sanket/AppShell";
+import { PageHeader } from "@/components/sanket/PageHeader";
 import { ChallengeBadge } from "@/components/sanket/ChallengeBadge";
 import { Chip } from "@/components/sanket/StatusBadge";
 import { Button } from "@/components/ui/button";
@@ -55,19 +56,19 @@ function ChallengeBrowse() {
   return (
     <AppShell>
       <div className="mx-auto max-w-6xl space-y-6 px-4 py-8">
-        <header className="flex flex-wrap items-end gap-4">
-          <div className="mr-auto">
-            <h1 className="text-2xl font-semibold tracking-tight">Societal challenges</h1>
-            <p className="text-sm text-muted-foreground">
-              Community problems, AI-categorised by domain and routed to matching institutions.
-            </p>
-          </div>
-          <Button asChild>
-            <Link to="/challenges/new">
-              <Plus className="h-4 w-4" /> Submit a challenge
-            </Link>
-          </Button>
-        </header>
+        <PageHeader
+          icon={Lightbulb}
+          eyebrow="Community driven"
+          title="Societal challenges"
+          description="Community problems, AI-categorised by domain and routed to matching institutions."
+          actions={
+            <Button asChild>
+              <Link to="/challenges/new">
+                <Plus className="h-4 w-4" /> Submit a challenge
+              </Link>
+            </Button>
+          }
+        />
 
         <div className="grid gap-3 sm:grid-cols-[1fr_auto]">
           <Input
diff --git a/src/routes/challenges.new.tsx b/src/routes/challenges.new.tsx
index e8d8b29..711b6c8 100644
--- a/src/routes/challenges.new.tsx
+++ b/src/routes/challenges.new.tsx
@@ -3,8 +3,9 @@ import { useQueryClient } from "@tanstack/react-query";
 import { useServerFn } from "@tanstack/react-start";
 import { useState } from "react";
 import { toast } from "sonner";
-import { Loader2, MapPin } from "lucide-react";
+import { Loader2, MapPin, PenLine } from "lucide-react";
 import { AppShell } from "@/components/sanket/AppShell";
+import { PageHeader } from "@/components/sanket/PageHeader";
 import { Button } from "@/components/ui/button";
 import { Input } from "@/components/ui/input";
 import { Label } from "@/components/ui/label";
@@ -134,13 +135,12 @@ function NewChallenge() {
   return (
     <AppShell>
       <div className="mx-auto max-w-3xl space-y-6 px-4 py-8">
-        <header>
-          <h1 className="text-2xl font-semibold tracking-tight">Submit a societal challenge</h1>
-          <p className="text-sm text-muted-foreground">
-            AI will categorise the domain, score severity, check for duplicates and route it to matching
-            universities and innovation partners.
-          </p>
-        </header>
+        <PageHeader
+          icon={PenLine}
+          eyebrow="New submission"
+          title="Submit a societal challenge"
+          description="AI will categorise the domain, score severity, check for duplicates and route it to matching universities and innovation partners."
+        />
 
         <form onSubmit={onSubmit} className="panel space-y-4 p-5">
           <div className="space-y-1.5">
diff --git a/src/routes/dashboard.citizen.tsx b/src/routes/dashboard.citizen.tsx
index adb48cc..f8ef84c 100644
--- a/src/routes/dashboard.citizen.tsx
+++ b/src/routes/dashboard.citizen.tsx
@@ -1,7 +1,9 @@
 import { createFileRoute, Link } from "@tanstack/react-router";
 import { useQuery } from "@tanstack/react-query";
 import { useMemo, useState } from "react";
+import { UserRound } from "lucide-react";
 import { AppShell } from "@/components/sanket/AppShell";
+import { PageHeader } from "@/components/sanket/PageHeader";
 import { ReportForm } from "@/components/sanket/ReportForm";
 import { IssueMap } from "@/components/sanket/IssueMap";
 import { StatusBadge, Chip } from "@/components/sanket/StatusBadge";
@@ -49,12 +51,12 @@ function CitizenDashboard() {
   return (
     <AppShell>
       <div className="mx-auto max-w-6xl space-y-6 px-4 py-8">
-        <header>
-          <h1 className="text-2xl font-semibold tracking-tight">Citizen portal</h1>
-          <p className="text-sm text-muted-foreground">
-            Every report is hashed, GPS-checked and merged with nearby complaints within 50 metres.
-          </p>
-        </header>
+        <PageHeader
+          icon={UserRound}
+          eyebrow="Citizen access"
+          title="Citizen portal"
+          description="Every report is hashed, GPS-checked and merged with nearby complaints within 50 metres."
+        />
 
         <div className="grid gap-6 lg:grid-cols-[1fr_1.3fr]">
           {session ? (
diff --git a/src/routes/issues.$id.tsx b/src/routes/issues.$id.tsx
index 605284b..e3493a9 100644
--- a/src/routes/issues.$id.tsx
+++ b/src/routes/issues.$id.tsx
@@ -91,7 +91,7 @@ function IssueDetail() {
     <AppShell>
       <div className="mx-auto grid max-w-6xl gap-6 px-4 py-8 lg:grid-cols-[1.6fr_1fr]">
         <div className="space-y-6">
-          <div>
+          <div className="rounded-xl border bg-gradient-to-r from-secondary/70 via-secondary/25 to-transparent px-5 py-6 sm:px-7 sm:py-7">
             <div className="flex flex-wrap items-center gap-2">
               <StatusBadge status={issue.status} />
               <Chip>{issue.category}</Chip>
diff --git a/src/routes/worker.dashboard.tsx b/src/routes/worker.dashboard.tsx
index 789aac1..fd9812f 100644
--- a/src/routes/worker.dashboard.tsx
+++ b/src/routes/worker.dashboard.tsx
@@ -3,8 +3,9 @@ import { useQuery, useQueryClient } from "@tanstack/react-query";
 import { useServerFn } from "@tanstack/react-start";
 import { useState } from "react";
 import { toast } from "sonner";
-import { Camera, Loader2, Navigation } from "lucide-react";
+import { Camera, HardHat, Loader2, Navigation } from "lucide-react";
 import { AppShell } from "@/components/sanket/AppShell";
+import { PageHeader } from "@/components/sanket/PageHeader";
 import { Chip, StatusBadge } from "@/components/sanket/StatusBadge";
 import { EvidenceImage } from "@/components/sanket/EvidenceImage";
 import { Button } from "@/components/ui/button";
@@ -62,10 +63,12 @@ function WorkerDashboard() {
   return (
     <AppShell>
       <div className="mx-auto max-w-3xl space-y-4 px-4 py-6">
-        <header>
-          <h1 className="text-2xl font-semibold tracking-tight">Task queue</h1>
-          <p className="text-sm text-muted-foreground">{tasks.length} open tasks, highest priority first.</p>
-        </header>
+        <PageHeader
+          icon={HardHat}
+          eyebrow="Field operations"
+          title="Task queue"
+          description={`${tasks.length} open tasks, highest priority first.`}
+        />
         {tasks.map((task) => (
           <TaskCard key={task.id} task={task} />
         ))}
